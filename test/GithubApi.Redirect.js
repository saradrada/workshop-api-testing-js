/* const chai = require('chai');
const chaiSubset = require('chai-subset');
const config = require('./GithubApi.Config');

chai.use(chaiSubset);
const { expect } = chai;
const githubUserName = 'aperdomob';

describe('Scenario: Consumen HEAD Service', () => {
  describe('Given...', () => {
    describe('When...', () => {
      before(async () => {

      });
    });

    it('Then...', () => {
    });
  });
}); */
